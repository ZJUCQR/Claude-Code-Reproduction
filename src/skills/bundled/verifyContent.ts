// Content for the verify bundled skill.
// Source snapshot fallback: bundled markdown assets are missing, so expose
// empty content instead of failing module resolution at startup.

export const SKILL_MD = ''

export const SKILL_FILES: Record<string, string> = {}
